## Packages
reactflow | The core drag-and-drop library for the proof editor
katex | Rendering LaTeX math formulas
react-latex-next | React component for KaTeX
clsx | Utility for conditional class names (standard with tailwind-merge)
tailwind-merge | Utility for merging tailwind classes (standard)
date-fns | Formatting dates for the dashboard

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  serif: ["var(--font-serif)"],
  mono: ["var(--font-mono)"],
}
